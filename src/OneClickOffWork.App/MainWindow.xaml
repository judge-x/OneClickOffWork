<Window x:Class="OneClickOffWork.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="一键下班" Icon="Assets/AppIcon.ico" Width="1124" Height="700" MinWidth="1124" MinHeight="700" MaxWidth="1124" MaxHeight="700"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="NoResize" Background="{DynamicResource WindowBackgroundBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="56"
                            ResizeBorderThickness="0"
                            CornerRadius="18"
                            GlassFrameThickness="0"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Border CornerRadius="18" Background="{DynamicResource WindowBackgroundBrush}" BorderBrush="{DynamicResource BorderBrushSoft}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="56"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Margin="0,0,18,0"
                            shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button ToolTip="最小化" Style="{StaticResource WindowButton}" Click="Minimize_Click"/>
                    <Button ToolTip="最大化" Style="{StaticResource WindowMaxButton}" Margin="8,0,0,0" Click="Maximize_Click"/>
                    <Button ToolTip="关闭" Style="{StaticResource WindowCloseButton}" Margin="8,0,0,0" Click="Close_Click"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Margin="22,0,22,22">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="204"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Style="{StaticResource Card}" Padding="12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel>
                            <Button Command="{Binding NavigateHomeCommand}" Style="{StaticResource NavButton}" Margin="0,0,0,8">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE80F;" Style="{StaticResource NavIconText}"/>
                                    <TextBlock Text="首页" FontSize="15" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding NavigateTodayTasksCommand}" Style="{StaticResource NavButton}" Margin="0,0,0,8">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE9D2;" Style="{StaticResource NavIconText}"/>
                                    <TextBlock Text="工作分析" FontSize="15" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding NavigateRemindersCommand}" Style="{StaticResource NavButton}" Margin="0,0,0,8">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE8F1;" Style="{StaticResource NavIconText}"/>
                                    <TextBlock Text="下班管理" FontSize="15" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding NavigateSettingsCommand}" Style="{StaticResource NavButton}" Margin="0,0,0,8">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE713;" Style="{StaticResource NavIconText}"/>
                                    <TextBlock Text="设置" FontSize="15" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding AboutCommand}" Style="{StaticResource NavButton}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE946;" Style="{StaticResource NavIconText}"/>
                                    <TextBlock Text="关于" FontSize="15" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Margin="6,14,6,0">
                            <Border Height="1" Background="{DynamicResource BorderBrushSoft}" Margin="0,0,0,12"/>
                            <TextBlock Text="快捷键" Foreground="{DynamicResource SubtleTextBrush}" FontSize="12"/>
                            <TextBlock Text="Ctrl + Alt + D" Margin="0,4,0,0" FontWeight="SemiBold"/>
                        </StackPanel>

                        <Border Grid.Row="3"
                                Background="{DynamicResource InputBackgroundBrush}"
                                BorderBrush="{DynamicResource BorderBrushSoft}"
                                BorderThickness="1"
                                CornerRadius="14"
                                Padding="8">
                            <StackPanel>
                                <TextBlock Text="{Binding CalendarTitle}" FontWeight="SemiBold" HorizontalAlignment="Center" FontSize="13"/>
                                <UniformGrid Columns="7" Margin="0,8,0,4">
                                    <TextBlock Text="一" Style="{StaticResource CalendarWeekText}"/>
                                    <TextBlock Text="二" Style="{StaticResource CalendarWeekText}"/>
                                    <TextBlock Text="三" Style="{StaticResource CalendarWeekText}"/>
                                    <TextBlock Text="四" Style="{StaticResource CalendarWeekText}"/>
                                    <TextBlock Text="五" Style="{StaticResource CalendarWeekText}"/>
                                    <TextBlock Text="六" Style="{StaticResource CalendarWeekText}"/>
                                    <TextBlock Text="日" Style="{StaticResource CalendarWeekText}"/>
                                </UniformGrid>
                                <ItemsControl ItemsSource="{Binding CalendarDays}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Columns="7"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border x:Name="DayRoot" Width="18" Height="18" CornerRadius="9" Margin="0,1">
                                                <TextBlock Text="{Binding Text}"
                                                           FontSize="10"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"
                                                           Foreground="{DynamicResource TextBrush}"/>
                                            </Border>
                                            <DataTemplate.Triggers>
                                                <DataTrigger Binding="{Binding IsToday}" Value="True">
                                                    <Setter TargetName="DayRoot" Property="Background" Value="{StaticResource AccentBrush}"/>
                                                </DataTrigger>
                                            </DataTemplate.Triggers>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>

                <ContentControl Grid.Column="1" Content="{Binding CurrentView}" Margin="20,0,0,0"/>
            </Grid>

            <Border HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Margin="0,66,0,0"
                    Background="#EE1F2937"
                    CornerRadius="14"
                    Padding="18,10"
                    Visibility="{Binding IsToastVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock Text="{Binding ToastText}" Foreground="White" FontSize="14"/>
            </Border>
        </Grid>
    </Border>
</Window>
