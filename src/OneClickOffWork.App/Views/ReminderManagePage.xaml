<UserControl x:Class="OneClickOffWork.Views.ReminderManagePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="ReminderListItemStyle" TargetType="{x:Type ListBoxItem}">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="12"
                                Padding="14,12">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource BorderBrushSoft}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
                                <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ReminderFooterButton" TargetType="{x:Type Button}" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Width" Value="74"/>
            <Setter Property="MinHeight" Value="42"/>
            <Setter Property="Padding" Value="0,8"/>
        </Style>

        <Style x:Key="ReminderFooterNeutralButton" TargetType="{x:Type Button}" BasedOn="{StaticResource ReminderFooterButton}">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
        </Style>

        <Style x:Key="ReminderFooterDangerButton" TargetType="{x:Type Button}" BasedOn="{StaticResource ReminderFooterButton}">
            <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Style="{StaticResource Card}" Padding="18">
            <DockPanel>
                <TextBlock Text="下班管理" FontSize="24" FontWeight="Bold"/>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <Button Content="导入" Command="{Binding ImportCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"/>
                    <Button Content="导出" Command="{Binding ExportCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"/>
                    <Button Content="默认" Command="{Binding RestoreDefaultsCommand}" Style="{StaticResource SecondaryButton}"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Margin="0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource Card}" Padding="12" Margin="0,0,12,0">
                <ListBox ItemsSource="{Binding Items}"
                         SelectedItem="{Binding SelectedItem}"
                         ItemContainerStyle="{StaticResource ReminderListItemStyle}"
                         BorderThickness="0"
                         Background="Transparent"
                         Foreground="{DynamicResource TextBrush}"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="4"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Background="{StaticResource AccentBrush}"
                                        CornerRadius="2"
                                        Opacity="0.72"
                                        Margin="0,2,12,2"/>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Title}"
                                               FontWeight="SemiBold"
                                               FontSize="14"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Content}"
                                               Foreground="{DynamicResource SubtleTextBrush}"
                                               FontSize="12"
                                               Margin="0,4,0,0"
                                               TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource Card}">
                <Grid DataContext="{Binding SelectedItem}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="编辑下班事项" FontSize="20" FontWeight="SemiBold"/>
                    <StackPanel Grid.Row="1" Margin="0,18,0,0">
                        <TextBlock Text="标题" Foreground="{DynamicResource SubtleTextBrush}"/>
                        <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Margin="0,6,0,12"/>
                        <TextBlock Text="内容" Foreground="{DynamicResource SubtleTextBrush}"/>
                        <TextBox Text="{Binding Content, UpdateSourceTrigger=PropertyChanged}"
                                 Height="130"
                                 AcceptsReturn="True"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 Margin="0,6,0,12"/>
                        <CheckBox Content="启用" IsChecked="{Binding IsEnabled}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="14">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="新增" Command="{Binding AddCommand}" Style="{StaticResource ReminderFooterButton}" Margin="0,0,8,0"/>
                <Button Content="上移" Command="{Binding MoveUpCommand}" Style="{StaticResource ReminderFooterNeutralButton}" Margin="0,0,8,0"/>
                <Button Content="下移" Command="{Binding MoveDownCommand}" Style="{StaticResource ReminderFooterNeutralButton}" Margin="0,0,8,0"/>
                <Button Content="删除" Command="{Binding DeleteCommand}" Style="{StaticResource ReminderFooterDangerButton}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
